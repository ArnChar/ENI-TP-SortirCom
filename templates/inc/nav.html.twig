<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container">

        <a class="navbar-brand" href="{{ path('default_home') }}">
            <img class="nav_logo" src="{{ asset('img/logo.png') }}" width="48px" height="48px" alt="{{ 'app.label.title'|trans }}">
            {{ 'app.label.title'|trans }}
        </a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">

                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('ville_index') }}">{{ 'nav.label.city'|trans }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('campus_index') }}">{{ 'nav.label.campus'|trans }}</a>
                    </li>
                {%  endif %}

                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('default_home') }}">{{ 'nav.label.home'|trans }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('participant_modify') }}">{{ 'nav.label.profile'|trans }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('security_logout') }}">{{ 'nav.label.logout'|trans }}</a>
                    </li>
                {%  endif %}

            </ul>
        </div>
    </div>
</nav>
